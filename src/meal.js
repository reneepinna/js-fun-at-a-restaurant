function nameMenuItem(foodName) {
  //take in a name and create a delicious name
  return `Delicious ${foodName}`;
}

module.exports = {
  nameMenuItem,
  // createMenuItem,
  // addIngredients,
  // formatPrice,
  // decreasePrice,
  // createRecipe
}


